# 简历分析系统 - 项目框架分析

> 本文档由代码扫视自动生成，梳理项目整体架构、各端功能与技术栈。

---

## 一、整体架构概览

系统是一个 **"多模态 AI 模拟面试评测 + 智能招聘大数据"** 平台，面向高校学生、学校管理员、企业三类用户，提供 AI 模拟面试、简历分析、岗位匹配、能力图谱、求职市场等一体化服务。

### 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│  前端层 (React + TS + Vite, 端口 5173)                       │
│  ├─ 学生端 / 学校端 / 企业端 / 公共认证端                    │
│  └─ 通过 Vite Proxy 转发 /api → 8082, /api/job-skill-graph → 7576 │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌───────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│ 主后端 Flask       │  │ 图谱服务 FastAPI  │  │ Java Spring Boot │
│ full_app.py        │  │ job_skill_graph  │  │ ai-qa-backend     │
│ 端口 8082          │  │ _service.py      │  │ 端口 8081         │
│ 认证/岗位/视频/    │  │ 端口 7576         │  │ 岗位 CRUD（企业）│
│ 学习资源/图谱代理  │  │ LLM + Neo4j      │  │                  │
└────────┬──────────┘  └────────┬─────────┘  └────────┬─────────┘
         │                      │                      │
         ▼                      ▼                      ▼
   ┌──────────────────────────────────────────────────────┐
   │  数据层                                                │
   │  ├─ PostgreSQL (job_graph 库)：users / user_roles /     │
   │  │   job_postings / interview_records / resources 等    │
   │  └─ Neo4j 5.15 (Docker, 端口 7474/7687)：岗位-技能图谱 │
   └──────────────────────────────────────────────────────┘
                              ▲
                              │ LLM 调用
                              │
                   ┌──────────┴──────────┐
                   │ 阿里云百炼 DashScope  │
                   │ (通义千问 qwen)       │
                   └─────────────────────┘
```

### 目录结构

```
resume_analysis_system/
├─ backend/                  # 后端
│  ├─ app.py                 # Flask 精简版（岗位查询，端口 8081）
│  ├─ simple_app.py         # Flask 简易版
│  ├─ full_app.py           # Flask 主应用（端口 8082，认证/岗位/视频/资源/图谱代理）
│  ├─ boss.py                # Boss 直聘爬虫（DrissionPage）
│  ├─ job_skill_graph_service.py  # FastAPI 知识图谱服务（端口 7576）
│  ├─ requirements.txt      # Python 依赖
│  ├─ pom.xml / src/main/   # Java Spring Boot 模块（岗位 CRUD，端口 8081）
│  ├─ crawler/               # 爬虫样本与结果
│  └─ .env.example
├─ frontend/                 # 主前端（学生/学校/企业统一入口）
│  ├─ src/
│  │  ├─ App.tsx            # 路由总入口（含 ProtectedRoute 鉴权）
│  │  ├─ layouts/           # MainLayout / SchoolLayout / CompanyLayout
│  │  ├─ pages/
│  │  │  ├─ auth/           # Login / Register / ForgotPassword
│  │  │  ├─ student/        # 学生端 15+ 页面
│  │  │  ├─ school/         # 学校端
│  │  │  ├─ company/        # 企业端
│  │  │  └─ dev/            # 调试页
│  │  ├─ services/          # api.ts / graph_api.ts / job_api.ts
│  │  ├─ stores/            # Zustand: auth / userProfile
│  │  └─ data/              # 面试字段定义
│  ├─ vite.config.ts        # Vite 代理配置
│  └─ package.json
├─ frontend2/               # 备用/实验前端工程
├─ scripts/                 # 数据导入与数据库检查脚本
│  ├─ data_import/          # 导入岗位、构建图谱
│  ├─ db_check/             # 数据库检查
│  ├─ db_fix/               # 数据库修复
│  └─ db_test/              # 数据库测试
├─ docker-compose-neo4j-only.yml  # Neo4j 容器编排
├─ 启动.md                  # 快速启动指南
├─ README.md
└─ NEO4J_docket部署.md
```

---

## 二、三端用户体系

| 端 | 用户类型标识 | 布局组件 | 入口路由 |
|----|------------|---------|---------|
| 学生端 | `student` | `MainLayout` | `/home` |
| 学校端 | `school` | `SchoolLayout` | `/school/dashboard` |
| 企业端 | `company` | `CompanyLayout` | `/company/dashboard` |
| 公共认证 | - | 无布局 | `/login` `/register` `/forgot-password` `/school/login` |

鉴权由 `ProtectedRoute` 组件 + Zustand `useAuthStore` 实现，按 `userType` 路由隔离；token 存 localStorage，请求经 axios 拦截器注入 `Authorization: Bearer <token>`。

---

## 三、各端功能详解

### 1. 学生端（功能最丰富，15+ 模块）

| 路由 | 页面 | 功能说明 |
|------|------|---------|
| `/home` | Home | 学生首页看板：面试统计（完成数/提升幅度）、推荐资源、快捷提问入口、四大核心功能卡片（智能模拟面试 / 多模态能力评测 / 个性化反馈报告 / 学习资源推荐） |
| `/qa` | QA | AI 智能体问答：文字对话 + 语音输入，按日期分组的历史记录管理 |
| `/history` | QA(history) | 历史问答记录查看 |
| `/jobs` | Jobs | 求职市场：搜索/筛选/查看详情/申请岗位，展示热门与最新岗位 |
| `/job-list` | JobList | 岗位列表（含 mock 数据），支持进入视频面试 |
| `/knowledge-graph` | KnowledgeGraphPage | 岗位能力知识图谱：G6 渲染岗位-技能关系图，支持岗位筛选与技能详情 |
| `/job-matching` | JobMatchingPage | 人岗匹配：粘贴简历文本 → 匹配目标岗位 → 推荐岗位列表 |
| `/trend-analysis` | TrendAnalysisPage | 趋势分析：岗位/技能趋势统计图表，可选生成 AI 洞察（Markdown 渲染） |
| `/resume-analysis` | ResumeAnalysis | 智能体简历分析：上传简历 → 岗位匹配度图表（柱状/雷达/饼图）+ 多维度评分 |
| `/interview` | Interview | 智能模拟面试：按领域卡片选择岗位进入面试流程（支持 URL 参数自动选择） |
| `/mock-interview-qa` | MockInterviewQA | 模拟问答面试：预设问题库逐题作答，展示参考答案与进度 |
| `/mock-interview-record` | MockInterviewRecord | 模拟面试录制：摄像头录制 + 表情情绪分析（紧张度/专注度等）+ 综合评分 |
| `/interview-video-analysis` | InterviewVideoAnalysis | 面试视频分析：摄像头录制/上传视频 → 分析结果与提示卡片 |
| `/analysis` | Analysis | 技能评估分析：技能雷达图 + 面试记录列表 + 综合能力评估 |
| `/formal-interview` | FormalInterview | 正式面试入口页（简单引导，尚未实现完整流程） |
| `/report/:id` | Report | 个性化反馈报告：面试评分、技能雷达图、优势/改进建议、支持下载 |
| `/resources` | Resources | 学习资源推荐：按分类/类型浏览、搜索、收藏、个性化推荐 |
| `/profile` | Profile | 个人中心：编辑用户信息、上传头像、修改密码 |

### 2. 学校端

| 路由 | 页面 | 功能说明 |
|------|------|---------|
| `/school/dashboard` | SchoolDashboard | 学校数据看板：按日/周/月/年统计各公司/岗位面试人数趋势，展示今日总面试人数、参与企业数、累计面试人数 |
| `/school/profile` | SchoolProfile | 账号管理：编辑学校基础资料、修改登录密码 |

> 学校端有独立登录入口 `/school/login`（SchoolLogin，标题"学校管理平台"）。

### 3. 企业端

| 路由 | 页面 | 功能说明 |
|------|------|---------|
| `/company/dashboard` | CompanyJobs（企业首页） | 岗位管理：发布/编辑/删除岗位、查看总岗位数与招聘中岗位数统计 |
| `/company/resumes` | CompanyResumes | 简历接收：获取申请列表、统计并审核处理候选人简历 |
| `/company/interviews` | CompanyInterviews | 面试管理：查看/编辑候选人面试安排与反馈 |
| `/company/profile` | CompanyProfile | 账号管理：编辑企业信息、上传 Logo、修改密码 |

### 4. 公共认证端

| 路由 | 页面 | 功能说明 |
|------|------|---------|
| `/login` | Login | 统一登录入口（标题"多模态AI模拟面试评测"）：学生/学校/企业 Tab 切换，按类型跳转对应主页 |
| `/register` | Register | 注册页：按用户类型动态展示扩展字段（学生填学号/学校/专业/年级；学校填代码/类型/地址/官网；企业填代码/行业/规模等） |
| `/forgot-password` | ForgotPassword | 忘记密码 |

---

## 四、后端服务详解

### 4.1 主后端 Flask（`full_app.py`，端口 8082）

负责认证、岗位查询、视频记录、学习资源、图谱代理。共 31 个路由：

| 模块 | 路由 | 说明 |
|------|------|------|
| 认证 | `/api/auth/login` `/register` `/user` `/profile` | 登录（MD5 密码校验，返回 token + userInfo）、注册、获取当前用户、更新资料 |
| 岗位 | `/api/jobs/active` `/search` `/<id>` `/popular` `/latest` | 分页查询、搜索、详情、热门、最新 |
| 投递 | `/api/applications/apply` | 简历投递（mock 返回成功） |
| 视频 | `/api/video/skill-assessment` `/interview-records` (GET/单条 GET/DELETE) | 技能评估、面试记录列表与详情、删除记录 |
| 学习资源 | `/api/resources/stats` `/all` `/recommendations` `/recommendations/generate` `/recommendations/<id>/viewed` `/completed` `/favorites/check` `/favorites/add` `/favorites/remove` `/favorites` | 资源统计、列表、推荐、生成推荐、标记已看/完成、收藏增删查 |
| 图谱代理 | `/api/job-skill-graph/*` | 代理至 FastAPI 7576 端口的图谱服务 |

### 4.2 图谱服务 FastAPI（`job_skill_graph_service.py`，端口 7576）

基于 LLM + Neo4j 的岗位能力知识图谱核心服务：

| 路由 | 方法 | 说明 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/api/job-skill-graph/extract-and-normalize` | POST | LLM 规范化岗位与技能（核心技能/可选技能/技能分类/置信度） |
| `/api/job-skill-graph/build-graph` | POST | 构建岗位-技能图谱（写入 Neo4j） |
| `/api/job-skill-graph/stats` | GET | 图谱统计 |
| `/api/job-skill-graph/skill-analysis/{skill_name}` | GET | 技能分析 |
| `/api/job-skill-graph/job-analysis/{job_name}` | GET | 岗位分析 |
| `/api/job-skill-graph/search` | GET | 图谱搜索 |
| `/api/job-skill-graph/graph-data` | GET | 图谱可视化数据（节点/边） |
| `/api/job-skill-graph/popular-skills` | GET | 热门技能 |
| `/api/job-skill-graph/jobs/{skill_name}` | GET | 按技能查岗位 |
| `/api/job-skill-graph/recommend-jobs` | GET | 按技能推荐岗位 |
| `/api/job-skill-graph/match-resume` | POST | 简历-岗位匹配评分（技能分/经验分/学历分） |
| `/api/job-skill-graph/trend-data` | GET | 趋势数据 |
| `/api/job-skill-graph/trend-insight` | POST | 趋势 AI 洞察 |
| `/api/job-skill-graph/trend-analysis` | POST | 综合趋势分析（含 AI 洞察） |

### 4.3 Java Spring Boot（`backend/src/`，端口 8081）

`com.black.job` 包下的岗位 CRUD 服务，与 Flask 部分功能重叠：

| 路由 | 方法 | 说明 |
|------|------|------|
| `/api/jobs/create` | POST | 发布岗位 |
| `/api/jobs/{jobId}` | PUT/DELETE | 更新/删除岗位（需 companyId 校验） |
| `/api/jobs/company/{companyId}` | GET | 分页查询公司岗位 |
| `/api/jobs/company/{companyId}/stats` | GET | 公司岗位统计 |
| `/api/jobs/active` `search` `{jobId}` `popular` `latest` `health` `test` | - | 与 Flask 同名接口重叠 |

> 注：项目存在 Python 与 Java 双后端并行的历史遗留，`full_app.py`（8082）为主应用，Java（8081）主要服务于企业岗位管理。Vite 代理默认指向 8082。

### 4.4 爬虫（`boss.py`）

使用 **DrissionPage** 控制 Chrome 自动化登录 Boss 直聘，按 11 个关键词（python开发、java后端、前端开发、大数据开发、golang游戏开发、c/c++、小程序开发、技术运维、机器人、人工智能等）爬取岗位数据写入 `data.csv`，再经 `scripts/data_import/import_data.py` 导入 PostgreSQL。

---

## 五、技术栈总览

### 前端

| 分类 | 技术 |
|------|------|
| 框架 | React 18 + TypeScript 5 |
| 构建 | Vite 5 |
| UI 库 | Ant Design 5 + @ant-design/icons |
| 样式 | TailwindCSS 3 + PostCSS + autoprefixer |
| 状态管理 | Zustand 4（持久化中间件 persist） |
| 路由 | React Router DOM 6 |
| 网络 | Axios（三套实例：api / graph_api / job_api，统一 token 拦截） |
| 图表/图谱 | @ant-design/charts、@ant-design/plots、AntV G6 5（知识图谱） |
| 视频/人脸 | face-api.js、react-webcam（模拟面试录制与表情情绪分析） |
| 交互 | framer-motion（动画）、react-draggable、re-resizable |
| 渲染 | react-markdown（AI 洞察 Markdown 渲染） |

### 后端

| 分类 | 技术 |
|------|------|
| Web 框架 | Flask 2.3（主应用）+ FastAPI 0.111（图谱服务）+ Spring Boot 3.2.5（Java 岗位服务） |
| ASGI | uvicorn[standard] 0.30 |
| 数据库驱动 | psycopg2-binary（PostgreSQL）+ neo4j 5.20（Bolt 协议） |
| ORM/数据库 | Spring Data JPA + Hibernate（Java 侧，ddl-auto: update） |
| LLM | 阿里云百炼 DashScope（通义千问 qwen），httpx 异步调用 |
| 数据校验 | Pydantic 2.7 |
| 工具库 | Hutool 5.8（Java，JSON 处理）、python-dotenv |
| 爬虫 | DrissionPage（Chrome 自动化） |
| WebFlux | Spring WebFlux（响应式流式响应，用于 AI 问答流） |

### 数据存储

| 数据库 | 用途 | 部署 |
|--------|------|------|
| PostgreSQL（`job_graph` 库） | 用户/角色/岗位/面试记录/学习资源/收藏等业务数据 | 本地 localhost:5432 |
| Neo4j 5.15 | 岗位-技能知识图谱（Job/Skill 节点 + REQUIRES 关系） | Docker，端口 7474(HTTP)/7687(Bolt)，账号 neo4j/password123 |

### 开发与部署

- 前端开发：`cd frontend && npm run dev`（端口 5173，Vite 代理转发 API）
- 主后端：`python full_app.py`（端口 8082）
- 图谱服务：`python job_skill_graph_service.py`（端口 7576）
- Neo4j：`docker-compose -f docker-compose-neo4j-only.yml up -d`
- Java 服务：Maven 构建，端口 8081（application.yml 配置 UTF-8 编码与 Hikari 连接池）

---

## 六、数据流要点

1. **认证流**：前端 Login → Flask `/api/auth/login`（MD5 校验）→ 返回 token → Zustand persist 持久化 → 后续请求 axios 拦截器注入 Bearer token。
2. **图谱流**：学生端 KnowledgeGraph/JobMatching/TrendAnalysis → Vite 代理 `/api/job-skill-graph/*` → FastAPI 7576 → 调用 LLM 规范化 + Neo4j Cypher 读写。
3. **视频面试流**：react-webcam 采集 + face-api.js 表情分析 → 上传 → Flask `/api/video/interview-records` 存储 → Analysis/Report 页展示雷达图与评分。
4. **岗位数据流**：boss.py 爬虫 → data.csv → `scripts/data_import/import_data.py` → PostgreSQL `job_postings` 表 → Flask/Java 暴露查询接口 → 学生 Jobs 页与企业 CompanyJobs 页消费。

---

## 七、备注与遗留

- `frontend2/` 为备用/实验工程，未在主路由使用。
- `student/Resources.jsx` 与 `Resources.tsx` 并存，.jsx 为旧版。
- `FormalInterview.tsx` 仅为入口引导，完整正式面试流程未实现。
- Java 与 Python 后端在岗位接口上存在重叠，建议明确分工或收敛为单一后端。
- `.env.example` 需配置 `DASHSCOPE_API_KEY`、`DASHSCOPE_BASE_URL`、`LLM_MODEL` 等 LLM 凭证。
